[project]
name = "amuse-nbody6xx"
dynamic = ["version"]
requires-python = ">=3.7"
dependencies = [
    "amuse-framework"
]

[build-system]
requires = ["hatchling", "versioningit"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "versioningit"

[tool.versioningit.next-version]
method = "smallest"

[tool.versioningit.format]
distance = "{next_version}.dev{distance}+{vcs}{rev}"
dirty = "{base_version}+d{build_date:%Y%m%d}"
distance-dirty = "{next_version}.dev{distance}+{vcs}{rev}.d{build_date:%Y%m%d}"

[tool.hatch.build]
skip-excluded-dirs = true

[tool.hatch.build.targets.wheel]
include = ["amuse_nbody6xx/**/*.py"]
exclude = [
    "amuse_nbody6xx/packages",
    "amuse_nbody6xx/support",
    "amuse_nbody6xx/src",
    "amuse_nbody6xx/tests",
    "amuse_nbody6xx/debug.py"
    ]
artifacts = ["amuse_nbody6xx/nbody6xx_worker"]

[tool.pytest.ini_options]
pythonpath = ["amuse_nbody6xx/tests/", "../../../tests"]

testpaths = ["amuse_nbody6xx/tests"]

addopts = "--import-mode=append"    # test the installed package

