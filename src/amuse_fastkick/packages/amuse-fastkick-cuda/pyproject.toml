[project]
name = "amuse-fastkick-cuda"
dynamic = ["version"]
requires-python = ">=3.7"
dependencies = [
    "amuse-framework",
    "amuse-fastkick"
]

[build-system]
requires = ["hatchling", "versioningit"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "versioningit"

[tool.versioningit.next-version]
method = "smallest"

[tool.versioningit.format]
distance = "{next_version}.dev{distance}+{vcs}{rev}"
dirty = "{base_version}+d{build_date:%Y%m%d}"
distance-dirty = "{next_version}.dev{distance}+{vcs}{rev}.d{build_date:%Y%m%d}"

[tool.hatch.build]
skip-excluded-dirs = true

[tool.hatch.build.targets.wheel]
include = ["amuse_fastkick"]
exclude = ["amuse_fastkick/*"]
artifacts = ["amuse_fastkick/fastkick_cuda_worker"]

[tool.pytest.ini_options]
pythonpath = ["amuse_fastkick/tests/", "../../../tests"]

testpaths = ["amuse_fastkick/tests"]

addopts = "-k TestFastKickGPU"

